# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Test coverage
coverage
*.lcov

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.aider*
.vercel
.env.local
.env*.local
_*.mjs
kakao-partner-raw.log
kakao-stream.log
.vercelignore 2
files/
.claude/

# kakao cookie 백업도 절대 커밋 금지
.env.local*

# CSV exports (상담 데이터 — 커밋 금지)
exports/

# Q-A 데이터셋 추출물 (상담 원문·PII — 절대 커밋 금지). 샘플(≤20행)만 별도 커밋.
/qa-dataset.jsonl
qa-dataset*.jsonl
!analysis/outputs/data/qa_dataset_sample.jsonl

# Google 서비스계정 키 + 동기화 상태 (커밋 금지)
google-service-account.json
.sheets-sync-state.json

# 빌드타임 생성 (대시보드 스냅샷) — prebuild가 매 배포마다 새로 만듦
public/cs-stats.json

# 카카오 쿠키 갱신 스크립트의 실행 중 lock 파일 (이 기기 로컬 런타임 상태, 커밋 금지)
.kakao-cookie-refresh.lock
