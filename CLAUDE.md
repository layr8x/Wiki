# Communication Style (사용자 응답 규칙)

사용자(layr8x@gmail.com)는 개발 지식이 전무함. 모든 응답을 아래 규칙으로 작성한다.

## 필수 규칙

1. **결과 먼저, 이유 나중 (두괄식)** - "위키 작동함 ✓" 같은 결론부터 한 줄. 세부 기술 설명은 그 아래. **보고서·분석 문서도 항상 두괄식** - 맨 위에 결론/권고 1~3줄(또는 "한 줄 요약"·"한눈에 보기")을 두고, 근거·과정은 그 아래에 둔다.
2. **기술 용어 쓸 땐 즉시 풀이** - 처음 쓰는 용어마다 괄호로 "(= 무엇)" 추가.
   - 예: "RLS(= Row Level Security, 데이터베이스에서 누가 어떤 행을 볼 수 있는지 정하는 규칙)"
3. **비유 적극 활용** - DB는 "보관함", 마이그레이션은 "설계도면 갱신", deploy는 "신축 건물 입주" 식으로.
4. **사용자가 직접 해야 할 행동만 굵은 글씨** - 나머지는 참고용 회색 톤.
5. **영어 약어 한국어 풀어쓰기** - CI, PR, FK, RPC, JWT 등 처음 등장 시 풀어 설명.
6. **모르겠다고 하면 다시 풀어 설명** - "이건 무슨 뜻이야?" 한 마디면 비유 + 그림 같은 묘사로 재설명.
7. **분석·의사결정 문서 = 검증된 빅데이터/의사결정 방법론을 명시 차용** - CRISP-DM(분석 생애주기), 가설검정, 대조군 인과추론(이중차분), DMAIC, KPI 트리·North Star, 피라미드 원칙(두괄식), RICE 우선순위, 민감도(분모) 분석 등. 방법론 이름은 풀이와 함께 쓴다. **수치가 실측인지 추정인지 확정 전인지는 문장으로 풀어 쓴다(대괄호 등급 표시는 쓰지 않는다)**(허위/과장 분석 금지).
8. **전 구성원 공유 보고서·대시보드엔 비유·은유 표현 금지** - 천장·집·창·샜다·무게중심·정공법 같은 은유 대신 **평이한 직설 용어**(상한·한계·유입·우선순위·직접 구현 등)를 쓴다. (비유는 layr8x 본인 대상 채팅 설명에서만 - 규칙 3.)
9. **AI 특유의 특수기호 사용 금지** - 보고서·채팅 답변 어디에도 `§`(section 기호), `—`(em dash) 등을 쓰지 않는다. 절 번호는 "4장"·"4-1절"·"넷째" 등 한글로, 줄바꿈 강조가 필요하면 쉼표·마침표·괄호·일반 하이픈(-)으로 대체한다.

## 사용자 컨텍스트 (사실 자료)

- **운영하는 사이트**: https://sdij-wiki.vercel.app (사내 직원용 위키)
- **운영하는 서비스 2개**:
  1. **AMS 위키** (sdij-wiki) - 학원 운영 메뉴얼·가이드를 직원이 검색하는 사내 도구
  2. **카카오 상담 수집** (kakao-partner stream) - 카카오 비즈니스 채팅(학부모↔학원) 실시간 수집해 분석용 DB 저장
  - (은퇴: **카카오 챗봇** kakao-webhook - 2026-07 사용자 확정 은퇴. 저장하던 kakao_messages 테이블·집계 뷰가 이미 삭제돼 비기능 상태였음. 함수 소스·설정 문서 제거 완료. 다시 되살리지 말 것.)
- **사용 중인 서비스**:
  - **Supabase**: 데이터베이스(보관함) + 인증(로그인 관리)
  - **Vercel**: 사이트 호스팅(웹사이트 띄워주는 곳)
  - **GitHub**: 소스코드 저장소 (코드 백업 + 변경 이력)

## 응답 길이

- 짧은 질문엔 짧게 (한두 줄).
- 설명이 필요한 질문엔 단계별 번호 매겨서.
- 표·체크리스트 적극 활용.

---

---

# 무엇이 어디 있는가

긴 작업 규칙은 전부 스킬로 뺐다. 필요할 때만 열리고 평소에는 자리를 차지하지 않는다.
스킬 본체는 `.agents/skills/`에 있고 `.claude/skills/` 링크는 세션 시작 때 `scripts/install_pkgs.sh`가 복원한다.

| 무슨 일을 할 때 | 열리는 스킬 | 어디서 왔나 |
|---|---|---|
| 챗봇 문구·메뉴·말풍선·카드를 고칠 때 | `myclass-chatbot` | 옛 11·12·13장 |
| Figma 파일에서 노드를 만들거나 고칠 때 | `myclass-figma-ds` | 옛 Figma장·8장 |
| 만든 화면을 Figma 원본과 대조할 때 | `figma-screen-audit` | 옛 9·10·14·15장 |
| 카카오 수집이 멈췄거나 채널을 손볼 때 | `kakao-collect-ops` | 옛 16·17-2·22장 |
| 관리자 화면에 버튼·표를 넣을 때 | `astryx-frontend` | 옛 18·23장 |
| 글쓰기 도구나 설치된 스킬을 찾을 때 | `repo-tooling` | 옛 19·20·21장 |

옛 22장은 23장 뒤에 22-6부터 22-9까지 잘려 있었다. 옮기면서 순서대로 붙였다.

# 상시 규칙 (스킬이 열리기 전에도 적용)

- **글을 쓰거나 다듬기 시작하면** `analysis/글쓰기_가이드_김명준.md` 0장 순서부터. 문체 정본은 `analysis/문체지문_김명준.md`, 측정기는 `python3 tools/writing/ko_ai_score.py 글.md --mj`.
- **화면이나 UI를 새로 만들거나 크게 고칠 때는** 시키지 않아도 먼저 Lazyweb으로 실제 사례를 찾고 시작한다. 백엔드·수집·SQL·글쓰기·분석·색 미세조정에는 쓰지 않는다. **나가는 것은 검색어뿐이다.** 상담 내용, 카카오와 잔디 대화, 개인정보, 사내 문서 본문, 내부 화면은 올리지 않는다.
- **검증 통과 전 "완료" 보고 금지.** 눈대중 판정도 금지다. 훅이 막게 돼 있지만 훅은 마지막 방어선이다.
- **스킬은 참고용이고 우리 규칙이 우선이다.** 외부 스킬 127개가 `astryx-frontend`나 `myclass-chatbot`과 충돌하면 우리 것을 따른다.

# 17. ★★ 사용자 신원·인프라 정정 (2026-07-06, 반복 오류 → 영구 기록)

> 이 세션에서 두 가지를 반복해서 잘못 알고 있다가 사용자에게 직접 교정받음. 다음 세션에서 다시 틀리지 않도록 고정.

## 17-1. 사용자 신원 - "사장님" 아님

- **이름**: 김명준
- **소속**: (주)하이컨시 > TECH사업본부 > 플랫폼서비스팀 > 캠퍼스파트
- **직함**: 플랫폼 UX 디자이너 겸 PO(Product Owner)
- **호칭**: **"사장님"으로 부르지 말 것.** 이 문서 어디에도 "사장님" 호칭을 지시한 적 없음 - 이전 세션이 "회사 서비스 운영자"라는 맥락만 보고 임의로 붙인 잘못된 추정이었다. **"김명준님"으로 부를 것.**
- **1장의 "개발 지식이 전무함" 커뮤니케이션 스타일 규칙은 그대로 유지**(사용자가 이 규칙 자체를 바꾸라고 지시한 적 없음 - 직함만으로 임의로 말투를 바꾸지 말 것). 신원 정정과 응답 톤 규칙은 별개.

# 24. ★★ desk = 개인 편집국 (별도 저장소 github.com/layr8x/desk, 2026-09)

> CLAUDE.md 546줄이 세션마다 통째로 로드되던 걸 부서 단위 스킬로 나눈다. 21장 "절차는 스킬로"를
> 실제로 집행하는 구조. 참고 사례 cbrock84/headcount(부서 16 · 스킬 172).

- **★ 여기(Wiki)에 두지 않는다**: 회사를 옮겨도 그대로 쓰려고 개인 저장소로 분리했다.
  정본은 **`layr8x/desk`**. 이 저장소에는 설치해서 쓴다.
  ```
  /plugin marketplace add layr8x/desk
  /plugin install plain@desk      # 말 검사역. 설치하면 Stop 훅이 자동으로 붙는다
  ```
- **부서 9개**: 묶는 층 `chief`(편집장) / 다섯 분야 `persuade`(콘텐츠)·`product`(기획)·
  `screens`(디자인)·`numbers`(분석)·`build`(개발) / 막는 쪽 `plain`(말 검사역)·`verify`(검사역)
  / 교체 `site`(현장). 조직도는 desk 저장소의 `docs/org-chart.html`.
- **회사 고유 값은 desk에 넣지 않는다.** 카카오 수집·마이클래스·Astryx 같은 하이컨시 사실은
  이 저장소의 스킬로 간다. desk로 가는 것은 **어디서나 통하는 교훈**뿐이다.
  (옛 24장은 이걸 반대로 적어 뒀다. 그대로 했으면 desk가 회사 색에 물들어 이직 때 못 썼다.)
- **말 검사역이 실제로 막는다**: 용어사전(83개)에 있는 말이 풀이 없이 쓰였는지, `ko_ai_score --mj`
  위험 판정이 있는지. 실측: `analysis/2026-06_월간보고_wiki챗봇.md`가 줄표 13개로 막힘.
- **이전 완료(2026-09-03)**: CLAUDE.md 570줄을 95줄로 줄이고 6개 스킬로 옮겼다. 지도는 위 "무엇이 어디 있는가".
  desk 쪽에는 더 옮길 게 없다. 화면 깨짐·정합 실패·조용한 실패·대역 데이터 교훈은 이미 중립 문장으로 들어가 있다.
