# AMS 챗봇 1차 스코프 데이터 근거 보완 보고서

작성일: 2026-07-08
대상: 7월 8일 AMS 챗봇 킥오프 회의록에 대한 데이터 근거 보완
용어: 이 보고서에서 "AMS 챗봇"은 상담실장 등 내부 직원이 사용할 위키 기반 질의응답 도구를 가리킨다. 이미 별도로 분석을 완료한, 재원생이 직접 쓰는 "마이클래스 챗봇"(전화상담 흡수 목적)과는 서로 다른 프로젝트이므로 혼동하지 않도록 문서 전체에서 "AMS 챗봇"으로 통일한다.

## 배경 및 목적

7월 8일 회의에서 AMS 챗봇의 1차 개발 범위(위키 가이드 검색 기반 질의응답, 세션 유지 없음, 7월 16일 개발 완료)와 2차 고도화 방향(학생 데이터 조회 연동, 상담 자동 요약, 권한/비용 통제)이 합의되었다. 회의록은 방향성과 리스크를 정확히 짚었으나, 아래 네 가지는 실제 데이터로 검증되지 않은 상태로 남아 있었다.

1. "가장 문의 빈도가 높은 상담 카테고리"가 구체적으로 무엇인지
2. 그 카테고리들을 현재 위키 콘텐츠가 실제로 얼마나 커버하고 있는지
3. 토큰/비용 통제를 정말 새로 설계해야 하는지, 아니면 재사용 가능한 기존 코드가 있는지
4. 2차 고도화의 리소스 절감 효과를 어느 수준으로 기대하는 것이 합리적인지

이 보고서는 위 네 가지 질문 각각에 대해 저장소에 이미 존재하는 전화상담 분석 데이터와 Supabase 데이터베이스를 직접 조회하여 답한다. 코드 변경은 없으며, 모든 조회는 읽기 전용(SELECT)으로 수행했다.

## 요약 (결론 먼저)

**1. 최다빈도 상담 카테고리는 이미 파악되어 있다.** 출결 관리(30.2%), 입반 상담(22.5%), LIVE(14.7%) 순이며, 이 다섯 개 카테고리가 전체 상담량의 84.3%를 차지한다. 회의록의 "추천안"(BI 데이터 추출)은 신규 작업이 아니라 기존 분석 결과를 재사용하면 충분하다.

**2. 다만 콘텐츠 커버리지를 대조한 결과 심각한 공백이 발견됐다.** 상담량 2위인 LIVE(14.7%, 6개월 누적 67,151건)에 대응하는 위키 가이드가 사실상 없다. 위키의 두 가지 콘텐츠 소스(정식 가이드 135건, FAQ 형식 46건)를 모두 확인했지만 LIVE 관련 항목은 단 1건, 그마저 본문과 무관한 안내문뿐이었다. 1차 챗봇이 이 상태로 열리면 두 번째로 많이 들어오는 문의 유형에서 반복적으로 "답을 찾지 못하는" 경험이 발생할 가능성이 높다.

**3. 위키 실사용 흔적은 소수 콘텐츠에 쏠려 있고, 검색 로그 자체는 없다.** 가이드 135건 중 조회 이력이 있는 것은 20건(14.8%)뿐이다. 게다가 "직원이 위키에서 무엇을 검색했는지" 남기는 로그 테이블(search_logs 등)은 스키마만 있고 실제 데이터는 0건이다. 즉 2차 고도화 우선순위를 결정할 실사용 데이터 자체가 아직 존재하지 않는다.

**4. 토큰/비용 통제는 새로 설계할 필요가 없다.** 회의에서 리스크로 언급된 항목들은 이미 재원생용 AI 검색 기능(api/search-summary.js)에 구현되어 동작 중이다. IP당 분당 20회 요청 제한, 응답 최대 토큰 500~700 제한, 저비용 모델(Claude Haiku 4.5), 프롬프트 캐싱이 이미 적용되어 있다. 2차에서 필요한 작업은 이 제한 기준을 IP에서 로그인 사용자 단위로 바꾸는 것뿐이다.

**5. 2차 고도화의 ROI는 카테고리별로 편차가 클 것으로 예상된다.** 상담량 상위 카테고리 중 입반 상담, 대기자 관리, 퇴원 상담은 대부분 사람의 판단이나 접수 처리가 필요한 유형으로, 학생 데이터를 연동해도 완전 자동응답으로 이어지기는 어렵다. 반면 출결 관리는 조회성 비중이 높아 데이터 연동 효과를 가장 먼저 체감할 수 있는 카테고리다. 회의록의 "So What"이 지적한 우려("1차만으로는 상담 데스크를 대체하기 어렵다")는 데이터로도 뒷받침된다.

---

## 목차

1. 데이터 개요 및 방법론
2. 최다빈도 상담 카테고리 확인
3. 위키 콘텐츠 커버리지 진단
4. 위키 실사용 현황과 검색 로그 공백
5. 토큰 비용, 접근 제어의 기존 선례
6. 2차 고도화 ROI에 대한 신중론
7. 종합 결론
8. Next Action 제안
9. 데이터 출처 및 한계

---

## 1. 데이터 개요 및 방법론

| 등급 | 내용 | 근거 |
|---|---|---|
| 측정 | AMS 전화상담 카테고리별 건수(1~6월 누적 456,266건), Supabase guides 테이블의 실제 조회수·도움됨 수, 정적 검색 인덱스(guide-index.js) 구성, search_logs·guide_views·guide_feedback 테이블의 행 수 | Supabase 프로젝트(sdij-wiki)에 대한 직접 SQL 조회, 저장소 코드 직접 확인 |
| 추정 | 전화상담 카테고리 비중을 향후 위키 검색 수요의 대리 지표로 사용하는 것 | 위키 자체의 검색 로그가 없어 부득이 대리 지표를 사용, 실제 검색 패턴과는 차이가 있을 수 있음 |
| 미측정 | 직원이 위키에서 실제로 어떤 키워드로 검색하는지, 상담 1건당 소요 시간, 챗봇 도입 후 실제 리소스 절감량 | 로그 테이블이 비어 있거나(전부 0행), 아직 발생하지 않은 미래 이벤트이기 때문 |

방법론은 두 갈래로 진행했다. 첫째, 기존 AMS 전화상담 심층분석(analysis/myclass-chatbot/2026_AMS전화상담_1-6월_연간심층분석.md)의 카테고리·의도유형 분류 결과를 재사용하되, 이번에는 "고객이 앱에서 셀프 해결할 수 있는가"가 아니라 "상담실장이 위키에서 답을 찾아야 하는 유형인가"라는 다른 질문에 적용했다. 둘째, 위키의 실제 콘텐츠(Supabase guides 테이블과 정적 검색 인덱스 guide-index.js)를 상담 카테고리 키워드로 직접 대조해 커버리지 공백 여부를 확인했다.

---

## 2. 최다빈도 상담 카테고리 확인

AMS 전화상담 1~6월 누적 대분류 순위는 다음과 같다(기존 분석 재사용).

| 순위 | 카테고리 | 건수 | 비중 |
|---|---|---:|---:|
| 1 | 출결 관리 | 137,849 | 30.2% |
| 2 | 입반 상담 | 102,671 | 22.5% |
| 3 | LIVE | 67,151 | 14.7% |
| 4 | 퇴원 상담 | 39,668 | 8.7% |
| 5 | 정산·수납 관리 | 37,206 | 8.2% |
| 6 | 대기자 관리 | 31,538 | 6.9% |
| 7 | 재원생 진도·수업 | 13,922 | 3.1% |
| 8 | 기타 | 11,968 | 2.6% |
| 9 | 설명회·컨설팅 | 7,330 | 1.6% |
| 10 | 교재 수령 | 5,502 | 1.2% |

상위 5개(출결, 입반, LIVE, 퇴원, 정산)가 전체 상담량의 84.3%를 차지한다. 회의록의 "추천안"(최다빈도 카테고리 한정 BI 추출, 질의 패턴 분석)에 대한 답이 바로 이 표다. 하위 95개 세부 유형과 의도유형별(조회·처리·컨설팅·혼합) 분류, 실제 문의·답변 예시까지 기존 리포트에 이미 정리되어 있으므로, 신규 데이터 추출보다는 이 결과를 챗봇 프롬프트와 QA 테스트케이스 설계에 반영하는 작업으로 전환하는 것이 효율적이다.

---

## 3. 위키 콘텐츠 커버리지 진단

### 3-1. 두 가지 콘텐츠 소스와 그 관계

이번 조사 과정에서 위키의 검색 대상 콘텐츠가 한 곳이 아니라 두 곳으로 나뉘어 있다는 점을 확인했다.

- Supabase `guides` 테이블: 135건. 조회수·도움됨 수가 실제로 기록되는 정식 콘텐츠(SOP, 정책, 트러블슈팅 등)다.
- 정적 검색 인덱스(`api/_lib/guide-index.js`): 181건. 현재 운영 중인 AI 의미검색 기능이 실제로 참조하는 카탈로그로, 위 135건에 더해 FAQ 형식 46건이 추가로 포함되어 있다.

이 46건의 FAQ는 `guides` 테이블에 없기 때문에 조회수 추적이 되지 않지만, 내용을 직접 확인한 결과 "출석을 결석으로 변경하고 싶어요", "대기 명단은 어디서 확인하나요" 같은 실무형 질문·답변으로 구성되어 있어 실질적인 가치는 충분하다. 즉 위키 콘텐츠의 진짜 규모는 135건이 아니라 181건이며, 이 중 26%(46건)는 조회수 데이터가 전혀 없는 사각지대라는 점을 감안해야 한다.

### 3-2. 카테고리별 커버리지 대조

두 소스(정식 가이드 135건 + FAQ 46건) 모두를 상담 카테고리 키워드로 대조한 결과는 다음과 같다.

| 상담 카테고리(비중) | 위키 커버리지 | 상태 |
|---|---|---|
| 출결 관리(30.2%) | 출결 처리 가이드(조회수 512), QR 출석 인식 실패 트러블슈팅(423), 출석부 인쇄 가이드, FAQ 3건(출석 상태 변경, 보강코드 등) | 양호 |
| 입반 상담(22.5%) | 입반 처리 가이드(389), 전반 처리 가이드(156), 대기번호 관리 및 입반 전환 가이드(134), FAQ 4건(정원 불일치, 대기 명단 등) | 양호 |
| LIVE(14.7%) | "라이브(sdijon) 수강 데이터 이관 불가 안내" 1건(조회수 0)이 전부. FAQ 46건 중에도 LIVE 관련 항목 0건 | 공백 |
| 퇴원 상담(8.7%) | 미납자 퇴반처리 방법(145), FAQ 일부(퇴원 후 출결기록 등) | 부분 |
| 정산·수납 관리(8.2%) | 환불 승인 기준 판단 가이드(567), 청구 생성 가이드(215), 전환결제 처리 가이드(187) 등 다수, FAQ 9건(환불·결제 관련) | 양호 |
| 대기자 관리(6.9%) | 대기번호 관리 및 입반 전환 가이드(134), 그 외 대기 관련 가이드는 조회수 0, FAQ 2건 | 부분 |

가장 중요한 발견은 LIVE다. 6개월 누적 67,151건(전체 상담량의 14.7%, 6월 한 달만 7,289건)을 차지하는 카테고리인데, 정식 가이드와 FAQ를 합쳐도 실질적인 대응 콘텐츠가 없다. 1차 챗봇이 이 상태로 7월 16일에 열리면, 상담량 2위 카테고리에서 챗봇이 반복적으로 답을 찾지 못하는 상황이 발생할 수 있다. 콘텐츠 보강(LIVE 관련 SOP·FAQ 작성)이 챗봇 개발과 병행되지 않으면 1차 오픈 직후 이 영역에서 부정적 피드백이 집중될 위험이 크다.

근거: Supabase `guides` 테이블 및 `api/_lib/guide-index.js` 직접 조회, 제목·모듈·태그 기준 키워드 대조.

---

## 4. 위키 실사용 현황과 검색 로그 공백

### 4-1. 실사용은 소수 콘텐츠에 집중

| 항목 | 값 |
|---|---:|
| 정식 가이드 수(guides 테이블) | 135건 |
| 조회수 1 이상인 가이드 | 20건(14.8%) |
| 조회수 0인 가이드 | 115건(85.2%) |
| 누적 조회수 합계 | 5,487회 |
| 누적 도움됨 합계 | 418회 |

상위 조회 가이드는 "2026 수강료 정책 변경 공지"(612회), "환불 승인 기준 판단 가이드"(567회), "출결 처리 가이드"(512회), "상황별 CS 대응 매뉴얼"(489회), "QR 출석 인식 실패 트러블슈팅"(423회) 순이다. 나머지 115건(청구·수납 모듈의 기능 업데이트 로그성 문서 다수 포함)은 지금까지 한 번도 조회되지 않았다.

시사점: 1차 챗봇의 답변 품질 검증은 135건 전체를 균등하게 볼 필요가 없다. 이미 실사용이 검증된 이 20건과, 3장에서 확인한 공백 영역(LIVE 등)에 검증 역량을 우선 배분하는 것이 효율적이다.

### 4-2. 검색 로그 자체가 존재하지 않음

`search_logs`, `guide_views`, `guide_feedback` 세 테이블을 직접 조회한 결과 모두 0행이다. 스키마는 2026년 5월 26일 마이그레이션으로 이미 만들어져 있지만, 실제로 로그가 쌓이고 있지 않다. 즉 "직원이 위키에서 무엇을 검색했는지", "AI 의미검색에 어떤 질문을 던졌는지"를 보여주는 데이터가 이 시점까지 전혀 없다.

이 보고서의 2장(최다빈도 카테고리)은 이 공백을 메우기 위해 전화상담 데이터를 대리 지표로 사용한 것이다. 다만 실제 위키 검색 패턴은 전화상담 패턴과 다를 가능성이 있다. 예를 들어 상담실장은 자주 처리하는 절차는 이미 숙지하고 있어 위키를 찾지 않고, 오히려 드물게 발생하는 예외 상황에서만 위키를 검색할 수도 있다. 1차 챗봇 배포 시점부터 검색 질의와 결과를 실제로 로그에 남기도록(이미 만들어진 search_logs 테이블에 insert 로직 연결) 개발 범위에 포함할 것을 권고한다. 이렇게 하면 2차 고도화 우선순위를 추측이 아닌 실측 데이터로 정할 수 있다.

---

## 5. 토큰 비용, 접근 제어의 기존 선례

회의에서 "향후 새로 설계가 필요하다"고 언급한 항목들은 이미 이 저장소의 다른 기능(api/search-summary.js, 재원생 앱의 AI 검색)에 동작 중인 코드로 존재한다.

| 회의에서 언급한 리스크 | 기존 코드의 대응 방식 |
|---|---|
| 분당 토큰 사용량 제어, 악의적·과도한 쿼리 방지 | IP당 분당 20회 요청 제한(인메모리 버킷) |
| 비용 폭증 방지 | 응답 최대 토큰 500~700으로 제한, 저비용 모델(Claude Haiku 4.5) 사용, 프롬프트 캐싱으로 반복 비용 절감 |
| 요청 크기 제한 | 요청 본문 8KB 상한 |

2차 고도화에서 새로 설계가 필요한 부분은 이 패턴을 "직원별·등급별 권한 제어"까지 확장하는 것뿐이다. 기존 코드는 IP 기준 제한이라 사용자(직원) 식별 기준이 아니다. 회의에서 언급한 "AMS 메뉴 권한과 챗봇 데이터 접근 권한의 1:1 매핑"을 구현하려면 IP 기준에서 로그인 사용자 기준 제한으로 바꾸는 작업이 필요하며, 신규 설계가 필요한 범위는 이 부분으로 한정된다.

---

## 6. 2차 고도화 ROI에 대한 신중론

회의록의 "So What"은 "1차 단순 조회 기능만으로는 현업의 업무 프로세스를 혁신하거나 상담 데스크 인력을 대체하기 어렵다"고 지적했다. 이를 기존 전화상담 분석으로 정량화하면 다음과 같다.

| 카테고리(비중) | 의도유형 구성(6월 실측) | 해석 |
|---|---|---|
| 출결 관리(30.2%, 1위) | 조회 56.8%, 처리 41.1% | 조회 비중이 상대적으로 높아 2차 학생 데이터 연동 효과를 가장 먼저 체감할 수 있는 카테고리 |
| 입반 상담(22.5%, 2위) | 컨설팅 85.4%, 처리 8.1%, 조회 3.5% | 대부분 "어떤 반이 맞는지" 같은 사람의 판단이 필요, 학생 데이터를 연동해도 자동응답으로 완결되기 어려움 |
| 퇴원 상담(8.7%) | 처리 52.7%, 컨설팅 47.3% | 취소 사유 확인, 환불 규정 안내 등 처리와 판단이 혼재 |
| 대기자 관리(6.9%) | 처리 86.2%, 조회 13.8% | 대기 접수 자체가 액션이라 데이터 조회만으로는 완결되지 않음 |

2차에서 학생 데이터 조회를 연동했을 때 즉각적인 효과를 기대할 수 있는 곳은 출결 관리 정도다. 물량 기준 2위, 4위, 6위인 입반, 퇴원, 대기자는 조회를 넘어 사람의 판단이나 접수 처리까지 필요하므로 챗봇이 완전히 대체하기 어렵다. 리소스 절감 ROI를 산정할 때는 이 구성비를 반영해, "학생 데이터만 연동하면 상담이 크게 준다"는 낙관적 가정 대신 "출결 관련 문의의 조회 속도를 높인다" 수준으로 1차 목표를 좁혀 잡는 것이 안전하다.

근거: analysis/myclass-chatbot/2026_AMS전화상담_1-6월_연간심층분석.md 4장, data/2026-06_AMS_성공률_분류.csv

---

## 7. 종합 결론

이번 조사로 회의록의 방향성 자체는 데이터로 뒷받침된다는 점이 확인됐다. 다만 그대로 진행할 경우 두 가지 구체적인 위험이 있다.

첫째, 콘텐츠 공백이다. 상담량 2위 카테고리인 LIVE에 대응하는 위키 콘텐츠가 정식 가이드와 FAQ를 합쳐도 사실상 없다. 이 상태로 1차를 오픈하면 챗봇의 체감 완성도가 실제 개발 품질과 무관하게 낮게 평가될 수 있다.

둘째, 측정 공백이다. 위키 검색 로그가 전혀 쌓이고 있지 않아 2차 고도화 우선순위를 결정할 실측 데이터가 없다. 1차 개발과 함께 로그 적재를 시작해야, 2차 계획 시점에는 추측이 아닌 데이터로 판단할 수 있다.

이 두 가지를 1차 개발 범위에 반영하면, 7월 16일 목표 자체를 바꾸지 않고도 오픈 직후의 리스크를 크게 줄일 수 있다.

---

## 8. Next Action 제안

기존 회의록의 Next Action에 아래 항목을 추가하거나 구체화할 것을 제안한다.

| 구분 | 내용 | 근거 |
|---|---|---|
| 신규 | LIVE 관련 SOP·FAQ를 최소 3~5건 1차 개발과 병행 작성한다 | 상담량 2위(14.7%) 카테고리인데 정식 가이드·FAQ를 합쳐도 커버리지가 사실상 0건 |
| 신규 | search_logs 테이블에 실제 검색 질의를 기록하는 로직을 1차 배포 범위에 포함한다 | 현재 스키마만 있고 데이터가 없어 2차 우선순위를 실측으로 정할 수 없음 |
| 구체화 | 회의록의 "최다빈도 카테고리 BI 추출"은 기존 AMS 전화상담 분석(6개월, 95개 세부유형)으로 이미 충족되므로, 신규 추출 대신 이 데이터를 프롬프트·QA 테스트케이스 설계에 바로 반영한다 | 2장 참고 |
| 구체화 | 토큰·비용 통제는 새로 설계하지 않고 api/search-summary.js의 기존 패턴(IP당 분당 20회, 응답 최대 토큰 500~700)을 사용자 단위로 확장하는 범위로 좁힌다 | 5장 참고 |
| 구체화 | 2차 ROI 목표치는 카테고리별 의도유형 구성(6장 표)을 반영해 카테고리마다 다르게 설정한다. 출결 관리는 자동화 효과 기대치를 높게, 입반·퇴원·대기자는 조회 보조 수준으로 낮게 잡는다 | 6장 참고 |

---

## 9. 데이터 출처 및 한계

| 출처 | 내용 |
|---|---|
| Supabase guides 테이블(직접 SQL 조회, sdij-wiki 프로젝트) | 3장·4장 가이드 커버리지, 조회수, 도움됨 수 |
| api/_lib/guide-index.js(저장소 코드 직접 확인) | 3장 정적 검색 인덱스 구성(181건), FAQ 46건 분석 |
| Supabase search_logs·guide_views·guide_feedback 테이블(직접 SQL 조회) | 4장 실사용 로그 부재 확인 |
| analysis/myclass-chatbot/2026_AMS전화상담_1-6월_연간심층분석.md | 2장·6장 상담 카테고리 volume, 의도유형 구성 |
| api/search-summary.js, src/hooks/useAiSearch.js | 5장 기존 토큰·비용 통제 코드 |
| docs/chatbot-design.md, analysis/2026-06_월간보고_wiki챗봇.md | 프로젝트 배경, 기존 로드맵 대조 |

한계: 이 보고서의 카테고리 우선순위(2장)는 전화상담 데이터를 위키 검색 수요의 대리 지표로 사용한 추정치이며, 실제 위키 검색 로그가 쌓이기 전까지는 검증할 수 없다. 콘텐츠 커버리지 진단(3장)은 제목·모듈·태그 기준의 키워드 대조로, 본문 내용까지 정독한 전수 검증은 아니다. 이 보고서는 코드 변경 없이 데이터 조회만 수행했으며, Supabase 조회는 읽기 전용 SELECT만 사용했다.
