# 현장기록

2026-09-03 조사. 열 칸을 채웠고 그중 둘은 조회가 막혀 문서 기록으로만 채웠다.
`desk` 데스크의 다른 여덟 부서는 이 파일의 **역할 이름**으로만 참조한다.

⚠ **사람 칸의 실제 이름은 여기 적지 않는다.** `현장기록.사람.md`(추적 제외)에 따로 둔다.

| 역할 | 실제 도구와 주소 | 없거나 권한 없을 때 | 마지막 확인 |
|---|---|---|---|
| 이슈 트래커 | Jira `hiconsy.atlassian.net`, 프로젝트 키 `SERP`(티켓 형식 SERP-8270). 로컬 사본 `workflow-sync-hub/notes/Jira/` 383건 | **커넥터가 설치돼 있지 않음**(2026-09-03 확인: 연결된 커넥터 35개 중 Atlassian 없음. 디렉터리에는 Atlassian Rovo 가 설치 가능으로 뜬다. 조직이 실제로 막는지는 눌러 봐야 안다). 브라우저에서 CSV로 내보내 `~/jira-exports/Jira.csv`에 두면 매일 09:00 예약 작업이 노트로 만든다(인증 불필요). 우회 스킬(= 상황에 맞게 펼쳐지는 지침서) `atlassian-realtime`, `atlassian-api` | 2026-09-03 · 로컬 노트 마지막 동기화 2026-08-26 |
| 문서 정본 | Confluence 같은 사이트, 공간 `HCYPLAT` `FVSOL` `AMS`(FVSOL 루트 1378910256). Notion은 **개인 계정**(가입 팀 0, 공유 페이지 0) | 코드 저장소의 문서 파일과 변경 이력. Google Drive | Notion 조회 성공 · Confluence 전용 도구 없음 |
| 메신저 | Slack `hiconsy`. 공지 `#일반`(최초 채널) `#시대인재운영`. 협의 `#상담챗봇tf`(비공개, 2026-04-23) `#오프라인플랫폼_comm` `#플서실-정산팀`. 알림 `#ci-deploy-dev` `#앱-배포` `#serp-jira` 외 3 | 잔디(사내 병행). 메일. Confluence 공지 | 채널 검색 24회, 본문 미열람 |
| 데이터 보관함 | Supabase 조직 `uktqrlzsffeofmpouvls` · 프로젝트 `sdij-wiki`(bnszzjaupayakkahmwsu), `stockdesk`. 파일 버킷 `kakao-archive`. BigQuery `myclass-data-warehouse` 데이터셋 4개. 사내 데이터베이스(= 자료 보관함) 12개와 Redash 133개는 문서 기록만 | 관리형 PostgreSQL로 표 이전. 화면 사용량 통계로 대체. `information_schema`로 구조 회수 | Supabase·BigQuery 조회 성공 · ClickHouse 권한 거부(401) **미확인** |
| 코드 저장소(= 코드와 이력을 모아둔 곳) | GitHub `layr8x`, 접근 11개, 기본 가지 전부 `main`. 업무 `Wiki` `hiconsy` `sdijservice/cs` `sdijservice-cs` `portfolio_mj` `workflow-sync-hub` | `git remote -v`, 저장소 웹 화면 목록 | 2026-09-03 |
| 배포처 | Vercel `layr8x's projects`. 운영 `sdij-wiki.vercel.app` `hiconsy.vercel.app`. Supabase 엣지 함수(= 서버 없이 클라우드에서 도는 작은 프로그램) 26개. 예약 실행 세 곳(클라우드 안 예약 장치, GitHub Actions, 담당자 맥북) | `vercel.json`, `.github/workflows` 읽기 | 2026-09-03 |
| 화면 원본 | Figma **이 세션 도구 자체 결함**으로 결론(2026-09-03 확정). 브라우저 접속 정상, 같은 팀(1126045604486413073) 확인, 재연결해도 동일. 파일 식별자: 웹 `A3JqKGl3NJD7CIRtjj6aNj` · 앱 `QHirLhjv84tseIPTqt3mF6` · 디자인시스템 `AWBOevxn4v0sjp6w22PPco` · 챗봇 `6PSg6RlWrjpnNYk1zirmUp` | 김명준님이 브라우저에서 캡처해 파일로 전달 | **연동 결함, 우회로 전환** · 노드 생사 판정은 2026-06 기록이 마지막 **미확인** |
| 회의 기록 | Granola 개인 범위. 로컬 `portfolio_mj/12_회의록_녹취/`. Confluence 주간 위클리 2072379811 | 녹음만 받아 맥에서 전사. Confluence 화면 캡처. 참석자에게 확인 | 최근 30일 12건 · 폴더 구조 유료 제한 **미확인** |
| 사람 | **`현장기록.사람.md` 참조**(추적 제외). 통로는 Jira `SERP`, Slack `#plat_캠퍼스파트` `#plat_오플` `#상담챗봇tf` | 문서 머리말의 수신·참조 줄. 이슈의 보고자와 담당자. 회의 참석자 명단 | 담당 변경 여부와 **거부권 쥔 자리 미확인** |
| 표기 규칙 | `Wiki/CLAUDE.md` · `Wiki/AGENTS.md` · `portfolio_mj/00_메타_보안/문서작성-컨벤션.md` · 같은 폴더 `MJ-CONTEXT.md` `FIGMA-작업컨벤션.md` · `hiconsy/docs/계열-UX라이팅-방법론.md` | 편집기 전체 검색 | 2026-09-03 · **규칙 충돌 2건 미해결**(아래) |

## 지금 막혀 있는 것 둘

**화면 원본을 못 연다. 파일 권한이 아니라 이 세션 도구 자체 결함이다.** 2026-09-03 확정.
처음엔 "편집 권한이 없습니다"라는 오류를 보고 소유자에게 편집자 추가를 요청하려 했다. 그런데
김명준님이 브라우저에서는 네 파일 모두 정상 접속·편집된다고 확인해 줬고, whoami로 확인한 소속
팀과 파일이 뜬 팀 번호(1126045604486413073)가 일치했고, Figma 연결을 끊었다 다시 연결해도 이
세션의 도구는 여전히 네 파일 모두 막혔다. Figma 문서가 안내하는 접근 거부 원인 세 가지(파일
링크, 계정, 소속 플랜)를 전부 확인했는데 모두 정상이었다. 남는 설명은 이 세션 연동 자체의
결함이고, 여기서는 못 고친다.

우회로 전환했다. 화면 대조가 필요하면 브라우저 캡처를 파일로 받는다. 편집 권한을 요청하는
문서(`작업/figma-편집권한/`)는 더 필요 없어져 삭제했다.

**이슈 트래커를 프로그램으로 못 부른다. 다만 원인이 문서에 적힌 것과 다르다.**
문서 세 곳은 조직 정책이라 적었는데, 2026-09-03 에 재보니 **커넥터가 아예 설치돼 있지 않다.**
연결된 커넥터 35개 어디에도 Atlassian 이 없고, 설치 목록에는 Atlassian Rovo 가 설치 가능으로 뜬다.
조직이 실제로 막는지는 연결을 눌러 봐야 안다. 그건 김명준님만 할 수 있다.
지금 도는 우회는 사람이 내려받은 파일을 읽는 경로 하나다.

## 규칙 충돌 2건

**하나. 대괄호 표시.** `Wiki/CLAUDE.md`는 수치에 `[측정]` `[추정]` `[미측정]`을 붙이라고 하고,
다른 규칙은 대괄호 등급 표시를 쓰지 말라고 한다. 같은 문장에 둘 다 적용할 수 없다.

**둘. 소속과 직함.** 문서마다 캠퍼스파트와 오프라인플랫폼팀이 갈리고,
산출물 작성자 표기에 직함을 쓰지 말라는 메모와 직함이 적힌 문서가 같이 있다.

## 확인 못 한 것

- 화면 원본의 노드 식별자가 지금도 살아 있는지(마지막 확인 2026-06)
- ClickHouse 접근 권한
- 회의록 정본이 어디인지
- 승인 단계가 몇 단인지, 거부권을 쥔 자리가 어디인지
- 날짜 표기가 챗봇 `26/06/01`과 웹 `25.12.21`로 갈린다. 보고서·문서만이라도 `2026-06-01`로 통일할지
- 디자인시스템 이름이 Onyx에서 Lumen으로 바뀌는 중인데 승인 대기로 보인다. 확정 전엔 문서에 Lumen을 안 쓰는 걸로 잠정 처리 중
- 문서 정본이 Confluence인지 재확인 필요. Notion은 가입 팀 0·공유 페이지 0이라 개인 계정으로 잠정 판단(2026-09-03)

## 2026-09-03 세션 마무리, 로컬로 이어받을 때 볼 것

**desk(github.com/layr8x/desk, main)**: 이번 세션에 부서 10개·스킬 40개(콘텐츠·기획·디자인·분석·개발 각 5개,
편집장 3, 말 검사역·검사역 각 2, 현장 1, 인과추론 외부 5)로 채웠다. 조직도(`docs/org-chart.html`)를
refero.design의 editorial 기준(타이포그래피 위계·밀도 리듬)으로 재감사해 실결함 둘 고침(위계 뒤섞임,
빈 공간). 커밋(= 변경 이력을 저장한 시점) `6a8c459`까지 반영.

**남은 것 하나, 다음 세션이 마저 할 것**: 카탈로그 리서치가 앤트로픽 공식 저장소
`anthropics/knowledge-work-plugins`에서 후보 2개를 찾았다(2026-09-03).
- `product-management/competitive-analysis` → desk `product` 부서에 (경쟁 비교 방법론이 없던 빈틈)
- `design/ux-writing` → `screens` 부서에 넣는 걸 추천(화면 안 마이크로카피는 문서보다 화면 쪽에 가깝다는
  판단, 확정 아님). `persuade`로 갈 수도 있어 갈릴 경우만 사용자 확인
- 저장소는 이미 로컬에 얕은 클론돼 있다(`/home/user/anthropics/knowledge-work-plugins`, read-only).
  들여올 땐 `causal` 부서 때와 같은 절차(본문 전체 읽기, 자동실행 장치 없는지 확인, 커밋 고정)를 따른다

**밀도 리듬 재측정 결과, 판단 보류**: `render_audit.cjs`로 재니 방향 뒤집힘 33%·최대최소비 7.7배가
나왔는데, 그 도구의 기준값(60~80%·6~160배)은 마케팅 페이지 실측이다. org-chart.html은 마케팅 페이지가
아니라 내부 구조 문서라 이 숫자를 그대로 미달로 볼지는 판단이 안 끝났다. 다음 세션에서 이어서 볼 것

**Figma**: 도구 자체 결함으로 결론 확정(위 참조). 화면 대조 필요하면 브라우저 캡처 요청

**남은 확인 질문**: 위 "확인 못 한 것" 목록 전체. 사용자 답변 대기 중, 아직 하나도 안 옴
