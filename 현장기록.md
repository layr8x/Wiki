# 현장기록

2026-09-03 조사. 열 칸을 채웠고 그중 둘은 조회가 막혀 문서 기록으로만 채웠다.
`desk` 데스크의 다른 여덟 부서는 이 파일의 **역할 이름**으로만 참조한다.

⚠ **사람 칸의 실제 이름은 여기 적지 않는다.** `현장기록.사람.md`(추적 제외)에 따로 둔다.

| 역할 | 실제 도구와 주소 | 없거나 권한 없을 때 | 마지막 확인 |
|---|---|---|---|
| 이슈 트래커 | Jira `hiconsy.atlassian.net`, 프로젝트 키 `SERP`(티켓 형식 SERP-8270). 로컬 사본 `workflow-sync-hub/notes/Jira/` 383건 | **커넥터가 설치돼 있지 않음**(2026-09-03 확인: 연결된 커넥터 35개 중 Atlassian 없음. 디렉터리에는 Atlassian Rovo 가 설치 가능으로 뜬다. 조직이 실제로 막는지는 눌러 봐야 안다). 브라우저에서 CSV로 내보내 `~/jira-exports/Jira.csv`에 두면 매일 09:00 예약 작업이 노트로 만든다(인증 불필요). 우회 스킬(= 상황에 맞게 펼쳐지는 지침서) `atlassian-realtime`, `atlassian-api` | 2026-09-03 · 로컬 노트 마지막 동기화 2026-08-26 |
| 문서 정본 | Confluence 같은 사이트, 공간 `HCYPLAT` `FVSOL` `AMS`(FVSOL 루트 1378910256). Notion은 **개인 계정**(가입 팀 0, 공유 페이지 0) | 코드 저장소의 문서 파일과 변경 이력. Google Drive | Notion 조회 성공 · Confluence 전용 도구 없음 |
| 메신저 | Slack `hiconsy`. 공지 `#일반`(최초 채널) `#시대인재운영`. 협의 `#상담챗봇tf`(비공개, 2026-04-23) `#오프라인플랫폼_comm` `#플서실-정산팀`. 알림 `#ci-deploy-dev` `#앱-배포` `#serp-jira` 외 3 | 잔디(사내 병행). 메일. Confluence 공지 | 채널 검색 24회, 본문 미열람 |
| 데이터 보관함 | Supabase 조직 `uktqrlzsffeofmpouvls` · 프로젝트 `sdij-wiki`(bnszzjaupayakkahmwsu), `stockdesk`. 파일 버킷 `kakao-archive`. BigQuery `myclass-data-warehouse` 데이터셋 4개. 사내 데이터베이스(= 자료 보관함) 12개와 Redash 133개는 문서 기록만 | 관리형 PostgreSQL로 표 이전. 화면 사용량 통계로 대체. `information_schema`로 구조 회수 | Supabase·BigQuery 조회 성공 · ClickHouse 조직 조회는 401 "invalid user"로 재확인(2026-09-04) — 자격 증명이 무효라는 뜻, 재연결은 김명준님만 가능(claude.ai 커넥터 설정 또는 `/mcp`). **재연결 전까지 계속 막힘** |
| 코드 저장소(= 코드와 이력을 모아둔 곳) | GitHub `layr8x`, 접근 11개, 기본 가지 전부 `main`. 업무 `Wiki` `hiconsy` `sdijservice/cs` `sdijservice-cs` `portfolio_mj` `workflow-sync-hub` | `git remote -v`, 저장소 웹 화면 목록 | 2026-09-03 |
| 배포처 | Vercel `layr8x's projects`. 운영 `sdij-wiki.vercel.app` `hiconsy.vercel.app`. Supabase 엣지 함수(= 서버 없이 클라우드에서 도는 작은 프로그램) 26개. 예약 실행 세 곳(클라우드 안 예약 장치, GitHub Actions, 담당자 맥북) | `vercel.json`, `.github/workflows` 읽기 | 2026-09-03 |
| 화면 원본 | Figma. **파일 키는 전부 바뀌었다 — 아래 "피그마 파일 키" 절이 정본, 그 위 어떤 기록(메모리 포함)도 안 믿는다.** 원격 MCP(`mcp__a08f7b0d…`)는 구조적 결함이 아니라 정상 작동한다, 옛 키가 죽어 있었을 뿐 | 파일 소유자가 공유해야 하는 것은 여전히 있을 수 있음(개별 파일 단위로 다름) | 2026-09-04, 10개 파일 전부 원격 MCP로 열림 확인, 아래 표 |
| 회의 기록 | Granola 개인 범위. 로컬 `portfolio_mj/12_회의록_녹취/`. Confluence 주간 위클리 2072379811 | 녹음만 받아 맥에서 전사. Confluence 화면 캡처. 참석자에게 확인 | 최근 30일 12건 · 폴더 구조 유료 제한 **미확인** |
| 사람 | **`현장기록.사람.md` 참조**(추적 제외). 통로는 Jira `SERP`, Slack `#plat_캠퍼스파트` `#plat_오플` `#상담챗봇tf` | 문서 머리말의 수신·참조 줄. 이슈의 보고자와 담당자. 회의 참석자 명단 | 담당 변경 여부 **미확인** · 거부권 쥔 자리는 2026-09-04 `jira_changelog`(전량 덤프, 28,263행) 분석으로 답 나옴 — 아래 참조 |
| 표기 규칙 | `Wiki/CLAUDE.md` · `Wiki/AGENTS.md` · `10_work/hiconsy/macos_workspace/00_메타_보안/MJ-CONTEXT.md`(2026-09-03 문서가 적은 `portfolio_mj/...` 경로는 실재하지 않음, 이게 실제 경로) · `FIGMA-작업컨벤션.md` `hiconsy/docs/계열-UX라이팅-방법론.md`(2026-09-04 기준 위치 미확인, 못 찾음) | 편집기 전체 검색 | 2026-09-04 · **규칙 충돌 2건, 김명준님이 결정해 반영 완료**(아래) |

## 피그마 파일 키 — 2026-09-04 확정, 이 위 어떤 기록도 이걸로 대체한다

**김명준님이 이미 여러 세션에서 "키가 전부 바뀌었다"고 말했는데 그때마다 기록에 안 남고 사라졌다.**
채팅으로만 말하고 파일에 안 적으면 그 세션이 끝나는 순간 없어진다는 뜻 — 이번엔 여기 못 박는다.
"Figma가 도구 자체 결함"이라던 이전 결론도 틀렸다: 원격 MCP(`mcp__a08f7b0d…`)는 정상 작동하고,
문제는 그냥 죽은 키를 붙들고 있었던 것뿐이다. 아래 10개 전부 2026-09-04에 원격 MCP `get_metadata`로
직접 열어 확인했다 — **다음 세션은 아래 키만 쓰고, 이 문서의 다른 옛 기록·로컬 메모리의 옛 기록은
전부 무시한다.**

| 파일 | 키 | 최상위 페이지 | 비고 |
|---|---|---|---|
| 디자인시스템(Lumen) | `813VPuDDGKZF20nzUyNaoc` | `58:2763` 📋 cover | Onyx→Lumen 전환 확인됨(508 TOKENS·65 COMPONENTS·2 MODES·WCAG AA·v1.2·2026), 옛 키 `AWBOevxn4v0sjp6w22PPco`는 죽음 |
| myclass 관리자 | `i02FanxgDwCiyM1iVgWk9i` | `553:42221` ❖ COMPONENT | |
| myclass 프론트(웹) | `8cl0g8w2PnnyE23DeIm3Ca` | `58:2763` 📋 cover | 옛 키 `A3JqKGl3NJD7CIRtjj6aNj`와 동일 파일, [[figma-myclass-render-key-and-deleted-pages]]에 2026-09-02부터 이미 적혀 있었음(활용 안 됐던 것) |
| myclass 앱(QR·키오스크) | `tdM9VfVU8jLALN8TstImnz` | `24062:62748` → sdij-myclass_app | |
| myclass 새움대치 지점추가 | `AbiNCUJ0QQYzsbt80Y3zgk` | `0:1` 🧑🏼‍💻 Screen | 옛 키 `LTnQuR43CqVjDRp2pwOreX`(새움대치) 후속으로 추정 |
| myclass 운영도우미 챗봇 | `VgGqdAmLfeWAy8JN9FTjVo` | `1032:54` MYCLASS_챗봇 | 옛 키 `6PSg6RlWrjpnNYk1zirmUp`(챗봇) 후속으로 추정 |
| 라이브 플랫폼(sdij-live) | `jdft7AISunwYuoKUhfzE2A` | `101:18169` COVER | myclass 계열 밖, 이번에 처음 기록 |
| 멤버 프론트(sdij-member) | `StrQ3CaxlDPY5TefKff0Cg` | `0:1` Cover | 〃 |
| n 하이브리드 앱(sdij-n) | `O4ydYhDze6ND28GlLI60dY` | `0:1` COVER | 〃 |
| c 프론트(sdij-c) | `isQnynx6WTuxl8cBKMjN6I` | `19412:44476` 250620~ [전체] | 〃 |

노드 단위로 읽으려면 URL의 `node-id=17547-3745`를 `nodeId: "17547:3745"`로 바꿔서 `get_metadata`에 준다.

## 지금 막혀 있는 것 (2026-09-04 기준)

**이슈 트래커를 프로그램으로 못 부른다.** 커넥터가 아예 설치돼 있지 않다(연결된 커넥터 35개 중
Atlassian 없음). 조직이 막는지는 연결을 눌러 봐야 안다 — 그건 김명준님만 가능. 우회는 사람이
내려받은 파일을 읽는 경로 하나뿐.

**ClickHouse는 반쯤 풀렸다.** 401(자격 증명 무효)은 김명준님이 재연결해서 없어졌다. 그런데 조직
목록이 비어 있고 "대기 중인 조직 초대를 수락하거나 ClickHouse Cloud 콘솔에서 계정 설정을 마치라"는
응답이다 — 콘솔에서 한 단계 더 필요, 이것도 김명준님만 가능.

**ClickHouse도 막힘.** 조직 조회 API가 401 "invalid user"로 거부(2026-09-04 재확인) — 자격 증명이
무효라는 뜻. claude.ai 커넥터 설정에서 재연결 필요, 이것도 김명준님만 가능.

## 규칙 충돌 2건 — 2026-09-04 김명준님이 결정, 반영 완료

**하나. 대괄호 표시.** "수치에 저런 규칙 하지마"로 결정. `Wiki/CLAUDE.md`의 `[측정]`·`[추정]`·`[미측정]`
대괄호 표기 요구를 없애고, "수치가 실측인지 추정인지 확정 전인지는 문장으로 풀어 쓴다"로 바꿔 반영함.
기존 분석 문서 다수(`analysis/` 아래 여러 파일)엔 이미 대괄호 표기가 쓰여 있는데, 이번엔 규칙 문구만
고쳤고 기존 문서들을 소급해서 고치진 않았다 — 필요하면 별도로 요청할 것.

**둘. 소속과 직함.** "플랫폼서비스팀의 캠퍼스파트"로 결정. `Wiki/CLAUDE.md`는 이미 이 표기였고(줄 70),
틀린 쪽은 `10_work/hiconsy/macos_workspace/00_메타_보안/MJ-CONTEXT.md`(오프라인플랫폼팀이라 적혀
있었음) — 고쳐서 반영함. 직함 표기 여부(직함을 쓰지 말라는 메모와 직함이 적힌 문서가 같이 있던 부분)는
**이번에 안 물어봤다, 아직 안 풀림.**

## 오늘(2026-09-04) 로컬 세션이 확인한 것

**밀도 리듬 — 풀림.** `render_audit.cjs` 소스와 그 스킬 문서(`design-audit/SKILL.md`)를 직접 읽으니
"조밀한 색인·표 페이지엔 `--dense`를 붙여 경고로 낮춘다, 스스로 판단해서 붙이지 말고 구조적으로
그런 페이지인가만 보라"는 규칙이 이미 있었다. org-chart.html은 마케팅 페이지가 아니라 부서·역할을
나열하는 색인 페이지라 이 규칙에 해당한다고 보고 `--dense`로 재실행 — **판정: 미달 없음**
(방향 뒤집힘 33%·최대최소비 7.8배는 그대로지만 경고로만 처리, 잘림·대비·표적크기·가로스크롤·초점표시
전부 통과). 새 판단이 아니라 있던 규칙을 적용한 것.

**앤트로픽 스킬 2개 — 들여옴.** 실제 이름은 `competitive-analysis`/`ux-writing`이 아니라
`competitive-brief`/`ux-copy`였다(저장소에 그 이름 자체가 없었음, 검색해서 확인). 내용 대조로 확인:
- `competitive-brief`(`product-management/skills/`) → `desk/plugins/product/skills/competitive-brief/`.
  경쟁사 비교·포지셔닝·승패분석·시장추세 방법론, 원래 찾던 빈틈과 정확히 일치
- `ux-copy`(`design/skills/`) → `desk/plugins/screens/skills/ux-copy/`. 화면 안 마이크로카피(CTA·에러
  메시지·빈 상태·확인창) 전용이라 `persuade`가 아니라 `screens`가 맞음, 갈릴 이유 없어서 확인 안 물어봄

들이기 전 확인: 각 폴더에 `SKILL.md` 하나만 있고 훅·스크립트·MCP 서버 선언·셸 호출 0건, 저장소
Apache-2.0. 커밋 `f30dc63b57654ab9b80da56ff2d1645c86f1c2de`(2026-09-02)로 고정, 원문 무수정.
조직도 재생성함 — 부서 10 · 스킬 40(product·screens 각 5→6).

**회의록 정본 — Granola는 아니다.** 그레놀라 최근 30일 12건을 봤는데 전부 개인 범위·단독 참석자고,
업무 메모와 개인 사정 메모가 섞여 있다(예: 이직 고민류). 팀이 같이 보는 회의 기록으로 쓰기엔 안 맞는
성격이다. 그러니 팀 공유 회의록의 정본은 Confluence 위클리(2072379811) 쪽으로 보는 게 맞다 —
확정은 아니니 아니면 알려주시길.

**승인 단계·거부권 — 데이터로 답 나옴.** `jira_changelog`(전량 덤프, 28,263행, 50건 상한 없음 확인)
분석 결과: 사실상 승인 단계는 1단이다. SERP 등은 Review, BTS는 QA대기가 유일한 반려 지점이고 그
뒤(운영반영요청→운영검수요청)는 되돌아가는 일이 드문 순차 처리 큐에 가깝다. 그 반려·승인 결정을
가장 많이 내리는 사람은 **김명준님 본인**(4개 게이트 중 3개에서 1위, 27~42%) — 다만 그 중 70%는
본인이 담당자 겸 보고자인 자기 티켓의 자기 마감이라 "독립된 결재자"라기보다 "본인 작업 스스로 닫는
비중이 크다"는 쪽에 가깝다. 순수 제3자 검수는 5건뿐. 반면 개발→운영 제출(운영반영요청)은 이혜빈·
유정민·이용재 등 완전히 다른 사람들이 한다 — 제출자 그룹과 게이트를 닫는 사람은 분리돼 있다.

**날짜 표기 — 오해였다, 안 고침.** "챗봇 26/06/01" 부분을 실제로 찾아보니 `myclass-figma-ds/SKILL.md`
100행에 이미 **"사용자 결정(피그마 디자인대로), 데이터 정본은 YYYY-MM-DD 유지, 화면 표시만
`fmtYMD()`로 변환"**이라고 적혀 있었다 — 즉 이건 실수가 아니라 결제 내역 화면 하나에 한정된, 이미
내려진 의도된 디자인 결정이다. 그래서 **이 파일은 그대로 뒀다.** "보고서는 `2026-06-01`처럼 쓴다"는
글쓰기 규칙만 관례로 남기고("어 그렇게 해"는 이 관례를 승인한 걸로 처리), 화면 표시 규칙과는 별개임을
분명히 해 둔다. "웹 25.12.21" 쪽은 정확히 어느 파일인지 못 찾았다.

**Onyx→Lumen — 확정 반영.** `Wiki/docs/chatbot-design.md` 13행의 "(Onyx 정책)"을 "(Lumen 정책)"으로
고침. 저장소 전체에서 Onyx를 쓰는 곳은 이 한 곳과 `현장기록.md`(이 문단) 뿐이었다.

**화면 원본 노드 생사**: 완전히 살아있다고 단정은 못 하지만, "no edit access" 거부가 "파일 없음"이
아니라 "권한 없음" 응답이라 파일 자체는 서버에 여전히 존재하는 것으로 보인다. 실제로 열리는지는
위 Figma 접근 문제가 풀려야 확인 가능.

## 아직 안 풀린 것

- Figma·Jira 커넥터·ClickHouse — 위 "지금 막혀 있는 것" 참조, 전부 김명준님 쪽 조치 필요
- 직함 표기 여부(산출물 작성자에 직함을 쓸지) — 이번에 안 물어봄
- 회의록 정본이 Confluence라는 판단은 소거법이지 직접 확인은 아님
- 문서 정본이 Confluence라는 전제(Notion은 개인 계정으로 판단, 2026-09-03) — 이번에도 재확인 못 함,
  "뭔소린지" 답을 받아 질문이 불명확했던 것으로 보고 원래 판단을 유지 중. 틀렸으면 정정 필요
- `표기 규칙` 행의 `FIGMA-작업컨벤션.md`·`hiconsy/docs/계열-UX라이팅-방법론.md` 두 파일은 위치를
  못 찾았다(2026-09-03 문서가 적은 `portfolio_mj/...` 경로 자체가 실재하지 않아서 못 미더움 — 이
  드라이브는 한글 파일명 NFC/NFD 정규화 문제가 있는 곳이라 다음 세션은 정규화 비교로 다시 찾을 것)
